#include <iostream>

using namespace std;

int main()
{
    int k,n,s,p;
    cin >> k >> n >> s >> p;
    
    int i = 1;
    while (true) {
        if ( (i*p/k)*s >= n) break;
        i++;
    }
    
    cout << i << endl;
}
