#include <iostream>
#include <set>

using namespace std;

int main()
{
	int n;
	cin >> n;

	set<int> scores;
	int tmp;
	for (int i = 0; i < n; i++) {
		cin >> tmp;
		if (tmp != 0) scores.insert(tmp);
	}

	cout << scores.size() << endl;
}
