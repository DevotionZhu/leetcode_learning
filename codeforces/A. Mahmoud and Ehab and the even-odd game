#include <iostream>
#include <cstdlib>
#include <string>


using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	string inputNum;

	cin >> inputNum;

	int num = atoi(inputNum.c_str());
	
	cout << (num % 2 ? "Ehab" : "Mahmoud") << endl;
}
