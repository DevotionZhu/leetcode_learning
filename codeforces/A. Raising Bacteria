#include <iostream>

using namespace std;

int main()
{
    long long x;
    cin >> x;
    
    int count = 0;
    while (x > 0) {
        if (x & 1) count++;
        x >>= 1;
    }
    
    cout << count << endl;
}
