#include <iostream>

using namespace std;

int main()
{
	int a, b, c;
	cin >> a >> b >> c;
	
	if (2*a > b) {
		if (b*7 < c*4) {
			cout << (b/2)*7 << endl;
		} else {
			cout << (c/4)*7 << endl;
		}
	} else {
		if (4*a > c) {
			cout << (c/4)*7 << endl;
		} else {
			cout << a*7 << endl;
		}
	}
}
