#include <iostream>

using namespace std;

int main()
{
	int n;
	cin >> n;

	int t;
	cin >> t;
	int maxn = t, minn = t;
	
	int res = 0;
	while (cin >> t) {
		if (t > maxn) {
			maxn = t;
			res += 1;
		} else if (t < minn) {
			minn = t;
			res += 1;
		}
	}
	
	cout << res << endl;
}
