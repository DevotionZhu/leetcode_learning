#include <iostream>

using namespace std;

int main()
{
	int n, m, z;
	cin >> n >> m >> z;

	int result = 0;
	for (int i = 1; i <= z; i++) {
		if (i%n == 0 && i%m == 0) {
			result += 1;
		}
	}

	cout << result << endl;
}
