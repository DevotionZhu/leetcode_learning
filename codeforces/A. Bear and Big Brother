#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

int main()
{
	string a,b;
	int ta, tb;
	cin >> a >> b;
	
	ta = atoi(a.c_str());
	tb = atoi(b.c_str());

	int i = 1;
	while (ta <= tb) {
		ta *= 3;
		tb *= 2;
		i++;
	}
	cout << i << endl;
	return 0;
}
