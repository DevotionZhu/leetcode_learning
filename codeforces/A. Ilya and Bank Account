#include <iostream>

using namespace std;

int main()
{
	long long n;
	cin >> n;

	long long res;
	if (n >= 0) {
		res = n;
	} else {
		res = (n/100)*10 + n%10;
		if (res < n/10) res = n/10;
	}

	cout << res << endl;
}
