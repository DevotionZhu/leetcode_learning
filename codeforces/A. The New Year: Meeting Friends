#include <iostream>

using namespace std;

#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MAX(a,b) ((a) > (b) ? (a) : (b))
int main()
{
	int x1, x2, x3;
	cin >> x1 >> x2 >> x3;
	
	int minDistance = MAX(MAX(x1,x2), x3) - MIN(MIN(x1, x2), x3);
	cout << minDistance << endl;
}
