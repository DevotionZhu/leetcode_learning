#include <iostream>

using namespace std;

int main()
{
	long long n;
	cin >> n;

	int num = n % 10;
	if (num == 0) {
		cout << n << endl;
	} else if (num <= 5 ) {
		cout << (n - num) << endl;
	} else {
		cout << (n - num + 10) << endl;
	}
}
