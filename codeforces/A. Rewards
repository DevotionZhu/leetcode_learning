#include <iostream>

using namespace std;

int main()
{
    int a1, a2, a3;
    cin >> a1 >> a2 >> a3;
    
    int b1, b2, b3;
    cin >> b1 >> b2 >> b3;
    
    int n;
    cin >> n;
    
    int numa = (a1+a2+a3)%5 ? (a1+a2+a3)/5 + 1 : (a1+a2+a3)/5;
    int numb = (b1+b2+b3)%10 ? (b1+b2+b3)/10 + 1 : (b1+b2+b3)/10;
    if (n >= numa + numb) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }
}
