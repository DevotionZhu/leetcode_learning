#include <iostream>
#include <cstdlib>

using namespace std;

int main()
{
	int n;
	cin >> n;

	int x,y,z;
	int sum = 0;
	while (cin >> x >> y >> z) {
		if (x + y + z >= 2) sum++;
	}

	cout << sum << endl;
}
