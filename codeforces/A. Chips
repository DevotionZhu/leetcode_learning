#include <iostream>

using namespace std;

int main()
{
	int n, m;
	cin >> n >> m;


	int i = 1;
	int rem = m;
	while (rem >= i) {
		rem -= i;
		i++;
		if (i == n + 1) i = 1;
	}

	cout << rem << endl;
}